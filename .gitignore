# Python
.venv/
*.pyc
__pycache__/
.pytest_cache/
*.egg-info/

# Local env / secrets
.env
**/.env
client/.env

# SQLite dev data
code/server/data/*.db
server/data/*.db

# Eval harness — secrets and run output stay local
eval/secrets/*.env
eval/secrets/mihomo/
eval/results/*
!eval/results/.gitignore
code/eval/secrets/*.env
code/eval/secrets/mihomo/
code/eval/results/*
!code/eval/results/.gitignore

# IDE / agent local
.claude/
.cursor/
tmp-*.json
*_ingest_*.json

# Node (if added later)
node_modules/
dist/
*.tsbuildinfo

# Local bootstrap / runtime data
code/server/data/
**/bootstrap_api_key.txt

# Local UX walkthrough scratch (never commit)
tmp-*/

# Generated bilingual scratch under eval workers (if any)
code/eval/results/**/*.zh.md
