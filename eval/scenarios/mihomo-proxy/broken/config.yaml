# Broken: wrong mixed-port — agent must set mixed-port: 7890
mixed-port: 7899
allow-lan: false
mode: rule
log-level: info

proxies:
  - name: stub
    type: direct

proxy-groups:
  - name: PROXY
    type: select
    proxies:
      - stub

rules:
  - MATCH,PROXY
