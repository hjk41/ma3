# Environment-specific deploy configs (real hosts/paths/secrets) are LOCAL ONLY.
# Only the template is committed.
*.env
!*.env.sample

# Legacy per-environment deploy scripts kept locally as fallback, never committed.
# The generic deploy/deploy.sh + a local *.env config replaces these.
deploy_ma3_v1_202.sh
deploy_and_test_agents_202.sh
run_eval_scenarios_202.sh
