global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: ma3
    metrics_path: /metrics
    static_configs:
      # Prefer host-network Prometheus scraping uvicorn on loopback.
      # (Docker bridge cannot reach processes bound to 127.0.0.1.)
      - targets: ["127.0.0.1:8000"]
        labels:
          service: ma3
