import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen, waitFor, fireEvent } from '@testing-library/react';
import { KeysPage } from '../src/pages/KeysPage';
describe('KeysPage',()=>{ beforeEach(()=>{ vi.stubGlobal('fetch', vi.fn(async (url:string, opts:any={})=>{ if(opts.method==='POST') return {ok:true,status:200,text:async()=>JSON.stringify({raw:'secret-once',info:{key_id:'k2',label:'new',scope_libraries:null}})}; if(opts.method==='DELETE') return {ok:true,status:204,text:async()=>''}; return {ok:true,status:200,text:async()=>JSON.stringify([{key_id:'k1',label:'test',scope_libraries:null}])}; })) as any; }); it('opens create modal and shows raw secret', async()=>{ render(<KeysPage/>); await waitFor(()=>expect(screen.getByText('test')).toBeInTheDocument()); fireEvent.click(screen.getByText('Create key')); fireEvent.change(screen.getByPlaceholderText('agent name or machine purpose'),{target:{value:'new'}}); fireEvent.submit(screen.getByPlaceholderText('agent name or machine purpose').closest('form')!); await waitFor(()=>expect(screen.getByText('secret-once')).toBeInTheDocument()); }); });
