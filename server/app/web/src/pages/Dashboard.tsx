import { Link } from 'react-router-dom';
import { Card, Badge, Button } from '../components/ui';
import { useUser } from '../contexts/UserContext';
import { buildLoginUrl } from '../auth/loginUrl';

export function Dashboard() { const { whoami } = useUser(); const anon = !whoami || whoami.principal.kind === 'anonymous'; return <div className="max-w-5xl mx-auto px-8 py-8"><div className="mb-8"><h1 className="text-2xl font-bold text-slate-800">ma3 Knowledge Network</h1><p className="text-sm text-slate-500 mt-1">Verified memories, cases, roles, and API keys for agents.</p></div>{anon ? <Card title="Welcome"><p className="text-slate-600 mb-4">Sign in to manage your libraries and keys.</p><a href={buildLoginUrl()}><Button>Login with auth.zhilicon.com</Button></a></Card> : <><div className="grid grid-cols-3 gap-3 mb-8"><Link to="/libs" className="p-4 border border-slate-200 rounded-xl hover:border-blue-300 bg-white"><span className="text-2xl">📚</span><span className="block text-sm font-medium text-slate-700">Libraries</span></Link><Link to="/keys" className="p-4 border border-slate-200 rounded-xl hover:border-blue-300 bg-white"><span className="text-2xl">🔑</span><span className="block text-sm font-medium text-slate-700">Keys</span></Link>{whoami?.admin_bypass && <Link to="/admin" className="p-4 border border-slate-200 rounded-xl hover:border-blue-300 bg-white"><span className="text-2xl">🛡️</span><span className="block text-sm font-medium text-slate-700">Admin</span></Link>}</div><Card title="Effective libraries"><div className="space-y-2">{whoami?.libraries.map((l:any)=><Link key={l.library_id} to={`/libs/${l.library_id}`} className="flex items-center justify-between px-3 py-2 rounded hover:bg-slate-50"><span>{l.name || l.library_id}</span><Badge label={l.role} color={l.role==='admin'?'blue':l.role==='writer'?'green':'slate'} /></Link>)}</div></Card></>}</div> }
