services:
  app:
    image: python:3.11-slim
    ports:
      - "18082:5000"
    volumes:
      - ./workspace:/app
    working_dir: /app
    command:
      - bash
      - -c
      - pip install -q flask supervisor && supervisord -n -c /app/supervisord.conf
