# Broken: wrong mixed-port (7899) and allow-lan false blocks cross-container access
mixed-port: 7890
allow-lan: true
bind-address: "*"
mode: rule
log-level: info

proxies:
  - name: stub
    type: direct

proxy-groups:
  - name: PROXY
    type: select
    proxies:
      - stub

rules:
  - MATCH,PROXY
