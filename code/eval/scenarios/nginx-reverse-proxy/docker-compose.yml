services:
  backend:
    image: hashicorp/http-echo
    command: ["-text=hello-from-backend"]
    expose:
      - "5678"

  nginx:
    image: nginx:alpine
    ports:
      - "18080:8080"
    volumes:
      - ./workspace/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend
