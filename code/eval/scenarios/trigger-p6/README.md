# trigger-p6 — Read-only exploration (P6, negative control for hooks)

Sample tree under `workspace/sample-project/`. Agent should explore with read-only commands;
hooks must **not** block `ls`, `cat`, `git status`, etc.
