# trigger-p4 — Trivial question (P4, negative control)

No KB seed. Agent should **not** call `ma3_context` or write back.

Infra verify always passes; pass/fail is determined by transcript analysis.
